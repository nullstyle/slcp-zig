// Generated by capnpc-zig
// Source: overlay.capnp

const std = @import("std");
const capnpc = @import("capnpc-zig");
const message = capnpc.message;
const schema = capnpc.schema;
pub const slcp = @import("slcp.zig");

pub const CAPNP_SCHEMA_MANIFEST_JSON: []const u8 = "{\"schema\":\"overlay.capnp\",\"module\":\"overlay\",\"serde\":[{\"id\":12043320614067227758,\"type_name\":\"DontHave\",\"to_json_export\":\"capnp_overlay_dont_have_to_json\",\"from_json_export\":\"capnp_overlay_dont_have_from_json\"},{\"id\":17142066313544628530,\"type_name\":\"Frame\",\"to_json_export\":\"capnp_overlay_frame_to_json\",\"from_json_export\":\"capnp_overlay_frame_from_json\"},{\"id\":12047008333870637928,\"type_name\":\"Hello\",\"to_json_export\":\"capnp_overlay_hello_to_json\",\"from_json_export\":\"capnp_overlay_hello_from_json\"},{\"id\":15247491935055121328,\"type_name\":\"SlotState\",\"to_json_export\":\"capnp_overlay_slot_state_to_json\",\"from_json_export\":\"capnp_overlay_slot_state_from_json\"}]}";
pub fn capnpSchemaManifestJson() []const u8 {
    return CAPNP_SCHEMA_MANIFEST_JSON;
}

pub const Frame = struct {
    pub const WhichTag = enum(u16) {
        unset = 0,
        hello = 1,
        envelope = 2,
        getQset = 3,
        qset = 4,
        dontHave = 5,
        getSlotState = 6,
        slotState = 7,
        ping = 8,
        pong = 9,
    };

    pub const Reader = struct {
        _reader: message.StructReader,

        pub fn init(msg: *const message.Message) !Reader {
            const root = try msg.getRootStruct();
            return .{ ._reader = root };
        }

        pub fn wrap(reader: message.StructReader) Reader {
            return .{ ._reader = reader };
        }

        pub fn which(self: Reader) error{InvalidEnumValue}!WhichTag {
            return std.enums.fromInt(WhichTag, self._reader.readU16(0)) orelse return error.InvalidEnumValue;
        }

        pub fn getUnset(self: Reader) !void {
            if ((try self.which()) != .unset) return error.WrongUnionMember;
            return {};
        }

        pub fn getHello(self: Reader) !Hello.Reader {
            if ((try self.which()) != .hello) return error.WrongUnionMember;
            if (self._reader.isPointerNull(0)) return Hello.Reader{ ._reader = self._reader.emptyStruct() };
            const value = try self._reader.readStruct(0);
            return Hello.Reader{ ._reader = value };
        }

        pub fn getEnvelope(self: Reader) !slcp.Envelope.Reader {
            if ((try self.which()) != .envelope) return error.WrongUnionMember;
            if (self._reader.isPointerNull(0)) return slcp.Envelope.Reader{ ._reader = self._reader.emptyStruct() };
            const value = try self._reader.readStruct(0);
            return slcp.Envelope.Reader{ ._reader = value };
        }

        pub fn getGetQset(self: Reader) ![]const u8 {
            if ((try self.which()) != .getQset) return error.WrongUnionMember;
            if (self._reader.isPointerNull(0)) return &[_]u8{};
            return try self._reader.readData(0);
        }

        pub fn getQset(self: Reader) !slcp.QuorumSet.Reader {
            if ((try self.which()) != .qset) return error.WrongUnionMember;
            if (self._reader.isPointerNull(0)) return slcp.QuorumSet.Reader{ ._reader = self._reader.emptyStruct() };
            const value = try self._reader.readStruct(0);
            return slcp.QuorumSet.Reader{ ._reader = value };
        }

        pub fn getDontHave(self: Reader) !DontHave.Reader {
            if ((try self.which()) != .dontHave) return error.WrongUnionMember;
            if (self._reader.isPointerNull(0)) return DontHave.Reader{ ._reader = self._reader.emptyStruct() };
            const value = try self._reader.readStruct(0);
            return DontHave.Reader{ ._reader = value };
        }

        pub fn getGetSlotState(self: Reader) !u64 {
            if ((try self.which()) != .getSlotState) return error.WrongUnionMember;
            return self._reader.readU64(8);
        }

        pub fn getSlotState(self: Reader) !SlotState.Reader {
            if ((try self.which()) != .slotState) return error.WrongUnionMember;
            if (self._reader.isPointerNull(0)) return SlotState.Reader{ ._reader = self._reader.emptyStruct() };
            const value = try self._reader.readStruct(0);
            return SlotState.Reader{ ._reader = value };
        }

        pub fn getPing(self: Reader) !u64 {
            if ((try self.which()) != .ping) return error.WrongUnionMember;
            return self._reader.readU64(8);
        }

        pub fn getPong(self: Reader) !u64 {
            if ((try self.which()) != .pong) return error.WrongUnionMember;
            return self._reader.readU64(8);
        }

    };

    pub const Builder = struct {
        _builder: message.StructBuilder,

        pub fn init(msg: *message.MessageBuilder) !Builder {
            const builder = try msg.allocateStruct(2, 1);
            return .{ ._builder = builder };
        }

        pub fn wrap(builder: message.StructBuilder) Builder {
            return .{ ._builder = builder };
        }

        pub fn setUnset(self: *Builder, value: void) !void {
            self._builder.writeU16(0, 0);
            _ = value;
        }

        pub fn initHello(self: *Builder) !Hello.Builder {
            self._builder.writeU16(0, 1);
            const builder = try self._builder.initStruct(0, 2, 2);
            return Hello.Builder{ ._builder = builder };
        }

        pub fn initEnvelope(self: *Builder) !slcp.Envelope.Builder {
            self._builder.writeU16(0, 2);
            const builder = try self._builder.initStruct(0, 0, 2);
            return slcp.Envelope.Builder{ ._builder = builder };
        }

        pub fn setGetQset(self: *Builder, value: []const u8) !void {
            self._builder.writeU16(0, 3);
            try self._builder.writeData(0, value);
        }

        pub fn initQset(self: *Builder) !slcp.QuorumSet.Builder {
            self._builder.writeU16(0, 4);
            const builder = try self._builder.initStruct(0, 1, 2);
            return slcp.QuorumSet.Builder{ ._builder = builder };
        }

        pub fn initDontHave(self: *Builder) !DontHave.Builder {
            self._builder.writeU16(0, 5);
            const builder = try self._builder.initStruct(0, 1, 1);
            return DontHave.Builder{ ._builder = builder };
        }

        pub fn setGetSlotState(self: *Builder, value: u64) !void {
            self._builder.writeU16(0, 6);
            self._builder.writeU64(8, @bitCast(value));
        }

        pub fn initSlotState(self: *Builder) !SlotState.Builder {
            self._builder.writeU16(0, 7);
            const builder = try self._builder.initStruct(0, 1, 1);
            return SlotState.Builder{ ._builder = builder };
        }

        pub fn setPing(self: *Builder, value: u64) !void {
            self._builder.writeU16(0, 8);
            self._builder.writeU64(8, @bitCast(value));
        }

        pub fn setPong(self: *Builder, value: u64) !void {
            self._builder.writeU16(0, 9);
            self._builder.writeU64(8, @bitCast(value));
        }

    };
};

pub const Hello = struct {
    pub const Reader = struct {
        _reader: message.StructReader,

        pub fn init(msg: *const message.Message) !Reader {
            const root = try msg.getRootStruct();
            return .{ ._reader = root };
        }

        pub fn wrap(reader: message.StructReader) Reader {
            return .{ ._reader = reader };
        }

        pub fn getProtocolVersion(self: Reader) !u32 {
            return self._reader.readU32(0);
        }

        pub fn getNetworkIdPrefix(self: Reader) ![]const u8 {
            if (self._reader.isPointerNull(0)) return &[_]u8{};
            return try self._reader.readData(0);
        }

        pub fn getNodeId(self: Reader) ![]const u8 {
            if (self._reader.isPointerNull(1)) return &[_]u8{};
            return try self._reader.readData(1);
        }

        pub fn getCurrentSlot(self: Reader) !u64 {
            return self._reader.readU64(8);
        }

        pub fn getListenPort(self: Reader) !u16 {
            return self._reader.readU16(4);
        }

    };

    pub const Builder = struct {
        _builder: message.StructBuilder,

        pub fn init(msg: *message.MessageBuilder) !Builder {
            const builder = try msg.allocateStruct(2, 2);
            return .{ ._builder = builder };
        }

        pub fn wrap(builder: message.StructBuilder) Builder {
            return .{ ._builder = builder };
        }

        pub fn setProtocolVersion(self: *Builder, value: u32) !void {
            self._builder.writeU32(0, @bitCast(value));
        }

        pub fn setNetworkIdPrefix(self: *Builder, value: []const u8) !void {
            try self._builder.writeData(0, value);
        }

        pub fn setNodeId(self: *Builder, value: []const u8) !void {
            try self._builder.writeData(1, value);
        }

        pub fn setCurrentSlot(self: *Builder, value: u64) !void {
            self._builder.writeU64(8, @bitCast(value));
        }

        pub fn setListenPort(self: *Builder, value: u16) !void {
            self._builder.writeU16(4, @bitCast(value));
        }

    };
};

pub const DontHave = struct {
    pub const Reader = struct {
        _reader: message.StructReader,

        pub fn init(msg: *const message.Message) !Reader {
            const root = try msg.getRootStruct();
            return .{ ._reader = root };
        }

        pub fn wrap(reader: message.StructReader) Reader {
            return .{ ._reader = reader };
        }

        pub fn getKind(self: Reader) !u8 {
            return self._reader.readU8(0);
        }

        pub fn getId(self: Reader) ![]const u8 {
            if (self._reader.isPointerNull(0)) return &[_]u8{};
            return try self._reader.readData(0);
        }

    };

    pub const Builder = struct {
        _builder: message.StructBuilder,

        pub fn init(msg: *message.MessageBuilder) !Builder {
            const builder = try msg.allocateStruct(1, 1);
            return .{ ._builder = builder };
        }

        pub fn wrap(builder: message.StructBuilder) Builder {
            return .{ ._builder = builder };
        }

        pub fn setKind(self: *Builder, value: u8) !void {
            self._builder.writeU8(0, @bitCast(value));
        }

        pub fn setId(self: *Builder, value: []const u8) !void {
            try self._builder.writeData(0, value);
        }

    };
};

pub const SlotState = struct {
    const StructListReader = message.typed_list_helpers.StructListReader;
    const StructListBuilder = message.typed_list_helpers.StructListBuilder;

    pub const Reader = struct {
        _reader: message.StructReader,

        pub fn init(msg: *const message.Message) !Reader {
            const root = try msg.getRootStruct();
            return .{ ._reader = root };
        }

        pub fn wrap(reader: message.StructReader) Reader {
            return .{ ._reader = reader };
        }

        pub fn getSlot(self: Reader) !u64 {
            return self._reader.readU64(0);
        }

        pub fn getEnvelopes(self: Reader) !StructListReader(slcp.Envelope) {
            if (self._reader.isPointerNull(0)) return StructListReader(slcp.Envelope){ ._list = self._reader.emptyStructList() };
            const raw = try self._reader.readStructList(0);
            return StructListReader(slcp.Envelope){ ._list = raw };
        }

    };

    pub const Builder = struct {
        _builder: message.StructBuilder,

        pub fn init(msg: *message.MessageBuilder) !Builder {
            const builder = try msg.allocateStruct(1, 1);
            return .{ ._builder = builder };
        }

        pub fn wrap(builder: message.StructBuilder) Builder {
            return .{ ._builder = builder };
        }

        pub fn setSlot(self: *Builder, value: u64) !void {
            self._builder.writeU64(0, @bitCast(value));
        }

        pub fn initEnvelopes(self: *Builder, element_count: u32) !StructListBuilder(slcp.Envelope) {
            const raw = try self._builder.writeStructList(0, element_count, 0, 2);
            return StructListBuilder(slcp.Envelope){ ._list = raw };
        }

    };
};

